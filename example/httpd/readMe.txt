用例说明：
	httpd用例是用于对Web Server的本地数据处理。即改动网页内容后，可以直接在SDK应用层做修改，不用修改WebServer相关的底层代码。

测试方法：
	1.2M模块烧录该文件夹下的测试网页lpb_web.html；
	2.电脑连接在模块的AP上，或者连接在同一个局域网下；
	3.如果模块是AP模式则在浏览器中打开http://10.10.100.254；
	   如果模块是STA模式则在浏览器中输入模块的IP；

注意：
	应用层操作的关键字需要保证和网页中使用的一致；
